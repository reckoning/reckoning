production: &default
  database: <%= ENV["DB_NAME"] || 'reckoning' %>
  adapter: postgresql
  host: <%= ENV["DB_HOST"] || 'localhost' %>
  port: <%= ENV["DB_PORT"] || 5432 %>
  user: <%= ENV["DB_USER"] || 'root' %>
  password: <%= ENV["DB_PASSWORD"] %>
  pool: <%= ENV["DB_POOL_SIZE"] %>

staging:
  <<: *default

development:
  <<: *default
  database: <%= ENV["DB_NAME"] || "reckoning_dev#{ENV['WORKTREE_SUFFIX']}" %>

test:
  <<: *default
  database: <%= ENV["DB_NAME"] || "reckoning_test#{ENV['WORKTREE_SUFFIX']}#{ENV['TEST_ENV_NUMBER']}" %>
