production:
  secret_key_base: rails_secret_base
  devise: devise_secret
  sentry: sentry
  pingdom:
  ga:
development:
  secret_key_base: rails_secret_base
  devise: devise_secret
  sentry: sentry
  pingdom:
  ga:
test:
  secret_key_base: rails_secret_base
  devise: devise_secret
  sentry: sentry
  pingdom:
  ga:
