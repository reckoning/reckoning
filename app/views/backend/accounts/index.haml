- content_for :title do
  = I18n.t(:"meta.title.backend.account.index")
.row#accounts
  .col-xs-12.col-md-12

    .row
      .col-xs-12.col-md-8
        %h1
          = I18n.t(:"headlines.backend.account.index")

      .col-xs-12.col-md-4
        .btn-group.pull-right.resource-nav
          = link_to I18n.t(:"nav.subnav.new_account"), new_backend_account_path, class: "btn btn-default"

    .row
      .col-xs-12.col-md-6
        .pull-right
          = paginate @accounts

    = render partial: "backend/accounts/table", :@accounts => @accounts

    .row
      .col-xs-12.col-md-6
        .pull-right
          = paginate @accounts
