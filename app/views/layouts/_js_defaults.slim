javascript:
  I18n.defaultLocale = "#{I18n.default_locale}";
  I18n.locale = "#{I18n.locale}";
  AuthToken = "#{auth_token}";
  StripeKey = "#{Rails.application.secrets[:stripe]['public_key']}";
  ApiBasePath = "#{(api_domain if defined?(api_domain))}"
